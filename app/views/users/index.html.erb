<!-- NHO: FYI this view breaks if a user registers via devise since there is no 'username' field  -->
<h2>Users</h2>
<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.username, user %></li>
  <% end %>
</ul>
